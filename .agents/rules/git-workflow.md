# Git Staging & Push Rule

- Do NOT execute `git add`, `git commit`, or `git push` automatically.
- Only run Git commands to stage, commit, or push code when the user explicitly instructs you to do so.
